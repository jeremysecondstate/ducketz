# Authoritative startup pointer

The authoritative commands for Loop A, active Pricing, independent Options
capture, directional Loop B, and pricing-active Strategy now live in
`docs/datafetch-ml/current_start_command`.

Do not maintain or execute a second command list from this file. Consolidating
the startup sequence prevents phase, watchlist, feature-profile, and pricing
mode drift.
