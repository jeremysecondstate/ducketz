# H.Y.P.E.R. concept revision 2 — compact header

Generation mode: built-in image generation, editing the existing Paper and Powder concepts.

Revision: remove the large in-content H.Y.P.E.R. title and Latin/English tagline; retain the application navigation tab; place the mode toolbar directly below navigation and reclaim dashboard space. Both output images are 1586 × 992 pixels. These are design concepts, not implemented application screens.

## Paper

Reference: `C:/dev/ducketz/docs/hyperliquid-duckets-design-ui/hyper-2026-09-25/hyper-paper-command-center.png`

Output: `hyper-paper-command-center-v2.png`

Prompt:

Use case: precise-object-edit. Edit this existing financial dashboard UI screenshot with ONE layout revision only. Remove the entire redundant in-content branding header band immediately below the application's top navigation: delete the large H.Y.P.E.R. heading, its Latin subtitle, and the English translation. KEEP the small H.Y.P.E.R. tab in the top application navigation exactly as it is. KEEP the Paper/Powder subtabs. Collapse the removed roughly 60-pixel-tall header band: move the mode-subtab row, buttons, status strip, metrics and dashboard upward so the Paper/Powder subtabs now sit directly below the top application navigation with a normal small margin. Use the reclaimed vertical room to make the performance chart and trade-journal workspace taller. Keep the overall canvas dimensions, bottom footer, typography sizes, dark navy/mint colors, column widths, exact account roles, financial numbers, status text, forecast probabilities and every other feature unchanged. Do not replace the removed title with a new banner or leave a blank gap. Do not add any yellow markup. This is a subtle density improvement, not a redesign. Preserve crisp legible table text and original values. Paper remains selected, the SIMULATED badge stays visible, and Pause paper and Export remain on the right of the compact mode row. Keep the paper performance chart, existing trades and selected decision. Preserve the DESIGN CONCEPT footer.

## Powder

Reference: `C:/dev/ducketz/docs/hyperliquid-duckets-design-ui/hyper-2026-09-25/hyper-powder-command-center.png`

Output: `hyper-powder-command-center-v2.png`

Prompt:

Use case: precise-object-edit. Edit this existing financial dashboard UI screenshot with ONE layout revision only. Remove the entire redundant in-content branding header band immediately below the application's top navigation: delete the large H.Y.P.E.R. heading, its Latin subtitle, and the English translation. KEEP the small H.Y.P.E.R. tab in the top application navigation exactly as it is. KEEP the Paper/Powder subtabs. Collapse the removed roughly 60-pixel-tall header band: move the mode-subtab row, buttons, status strip, metrics and dashboard upward so the Paper/Powder subtabs now sit directly below the top application navigation with a normal small margin. Use the reclaimed vertical room to make the performance chart and trade-journal workspace taller. Keep the overall canvas dimensions, bottom footer, typography sizes, dark navy/mint colors, column widths, exact account roles, financial numbers, status text, forecast probabilities and every other feature unchanged. Do not replace the removed title with a new banner or leave a blank gap. Do not add any yellow markup. This is a subtle density improvement, not a redesign. Preserve crisp legible table text and original values. Powder remains selected, the REAL MONEY / Not connected state stays visible, and Execution unavailable and Export stay on the right of the compact mode row. Preserve all unconnected empty states, dashes, chart placeholder, empty trade journal, and existing shared forecast preview. It must remain the exact sibling layout of Paper. Preserve the DESIGN CONCEPT footer.

