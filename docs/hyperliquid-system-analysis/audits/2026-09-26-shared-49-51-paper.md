# Shared 49%/51% Paper thresholds — September 26, 2026

The user requested broader Paper entries without a fresh experiment, then
explicitly selected the same **49%/51% thresholds for exits**. Active config is
`entry_band=exit_band=0.01`. Flat and held long positions are eligible at or above
51%; flat and held shorts at or below 49%. Longs exit below 51%, shorts above 49%.
At the exact boundary both entry and retention use the same inclusive comparison,
avoiding repeated entry/exit caused solely by different boundary operators.

The previous validator required exit strictly below entry. Equal bands are now
supported. With equal bands, sizing conviction is `abs(p-0.5)/saturation_band`,
capped at one, when active. Subtracting the shared cutoff would otherwise assign
zero size to an eligible boundary. This changes sizing as well as eligibility;
no minimum target was invented. Historical unequal-band policies keep their
previous hysteresis and sizing curve. Qualification, minimum trades, risk caps,
cooldowns, stop-reference semantics, separate taker fees and zero added slippage
are unchanged. The initially suggested 49.5%/50.5% exits were never loaded by a
live worker; the user's shared-threshold choice superseded them before reload.

## Same experiment, new policy

Paper does not reload its config while running. PID75292 was gracefully stopped
and the ordinary runtime command resumed the existing ledger as actual PID48468,
venv launcher3484, hidden cmd72720 (process created12:57:01.879278 UTC). No mirror
read or reseed occurred. Experiment **20260926T123436Z-book-vwap**, seed
**2026-09-26T12:34:36.903077126Z**, and opening **42088.24361973616** are unchanged.
Old policy **2c8197e2cbe140d6** remains stored with its original journals.
New policy is **747825fed0f17c1c**. Current Paper config SHA-256 is
`faba6ddc2b8b18dbc82b7cd282a1dfc6468f7c1fd1fb123a82cebae4377d2a48`;
the other three configuration hashes are unchanged.

Read-only evidence under `C:/DATASTORE/hyperliquid/_operations`:

- `paper-policy-4951-before.json`: stopped state, original metadata, policy,
  seed, current accounts/positions, row counts and canonical journal hashes.
- `paper-policy-4951-after.json`: verified new owner and policy, matching original
  journal prefixes, unchanged opening hash, current health and source states.
- `paper-policy-4951-prior-maintenance.json`: preserved prior completed marker.

Before reload: 49 cycles, 8 fills, 24 decisions, no transfers/funding. At
**12:57:51.088287 UTC**, the new worker had advanced to51 cycles with the same8
fills and24 decisions. Cash and signed positions were byte-equivalent under
canonical row serialization; all earlier journal rows and opening data were
unchanged. New marks account for subsequent valuation changes. Already processed
forecasts remain consumed, so ordinary signal decisions await a new publication.
Data57004 and models56520 continued. All17 source states were fresh, warnings
were empty, and Powder remained inactive with zero pending intents.

**316 tests passed** across policy, runtime, market and ledger. Coverage includes
exact49%/51% execution and retention, just-inside exits, and resume preserving
seed/history/risk references/cooldowns while recording the new policy. Legacy
unequal-band tests remain intact. Operations Watch v8 adopts this policy phase
while retaining the original experiment opening.

The first subsequent publication was processed by **13:00:16.872525 UTC**:
12 decisions carried the new policy and matching 49%/51% entry/exit diagnostics,
producing three fills. Their prices still equaled book VWAP with zero extra
slippage. Remaining rows were four opposite-account-direction holds, three
unqualified-forecast exclusions, and two below-size-precision skips. No decision
was blocked by the old 46%/54% entry band. Evidence:
`_operations/paper-policy-4951-first-decisions.json`. Maintenance completion and
the synchronized v8 watch were published at13:00:26.226820 UTC.
