Superseded, inactive design; earlier opt-in Live-denial branch conflicts with final user requirement and must be removed before any integration.

These files were relocated from the active `ml` and `tests` directories after the user required successful matching gap evidence from both Databento APIs. No fallback was activated or integrated. XNAS Live denial does not meet that requirement, and the observed Schwab close remains outside the unchanged five-minute reference tolerance.

The draft passed 59 focused tests before the final policy decision. It is not production-ready; those results do not authorize integration or resolve the COST gap.
