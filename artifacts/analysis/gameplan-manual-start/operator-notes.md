# Manual Gameplan trader start — September 9, 2026

The user can run `C:/dev/ducketz/Start-Gameplan-Trader.cmd` once before the session. Their manual invocation activates the existing trading controls and starts the Gameplan direction worker. It waits until 04:00 Pacific on the next supported trading day and wakes automatically, with no second click or activation prompt. The first normal entry batch is 04:01. An invocation during the supported session starts immediately. The computer must remain running.

The worker reports `SLEEPING_UNTIL_OPEN`, its action date and wake time, and keeps its session lock and heartbeat while waiting. It makes no broker requests, inventory assignments, entry claims or orders during that waiting state. Deactivation or stopping the process cancels the wait. Market calendar handling covers weekends, holidays and Pacific daylight saving time using the existing supported-session rules.

The wrapper selects `gameplan-direction-current-market-v1`. This opt-in mode follows the published Gameplan direction: bullish at or above 54% buys, bearish at or below 46% sells available shares, and neutral holds. It uses current ask prices for buys and current bid prices for sells, literal current cash, current holdings, working-order reservations and actual fills. Planning price and cash ranges are estimates; being outside a planning range does not block execution. Current-price limit orders still depend on available market liquidity and can remain unfilled.

The direction engine uses the existing 1:2:3:4 horizon capacity weights without the legacy confidence multiplier. It prevents double allocation across horizons and does not spend expected sale proceeds before account evidence makes them available. The new mode accommodates the seven opening symbol sales in one batch. Existing learned and fixed sizing defaults remain available and unchanged.

Existing unallocated shares are assigned to a horizon only when its actual bearish sale is reserved, with explicit inventory records rather than fictitious purchase fills. Definitively rejected or cancelled sales release unsold assigned shares; uncertain submissions retain their reservation for reconciliation. Prior owned shares and filled portions remain accounted for. Current broker quote timestamps are retained and checked again immediately before submission.

The existing 03:55 Windows launcher recognizes and adopts a valid manually started waiting worker rather than creating a duplicate. Its default strategy is otherwise unchanged. The three existing Codex Scheduled prompts now recognize this manual mode, its automatic wake and estimated-range semantics. Their schedules, models, reasoning settings, notifications and other fields were preserved and read back successfully. Verification is saved in `automation-verification.json` in this directory. Seven documents in `docs/loops-system-analysis/` describe the completed process.

The September 9 Gameplan review now explicitly describes its price and cash columns as estimates. Its numerical tables were preserved exactly. The immutable source publication remains `C:/DATASTORE/ml/nightly-gameplan-runs/20260909T060404.450224Z`; the read-only preflight reported READY with all 133 entry forecasts qualified. Calendar verification gave September 9 at 04:00–17:00 Pacific. The current derived review SHA-256 is `7b52a6d809cf96e7850d56d341bd102d7a54cd5beee7d6a3afbca3627329ce36`.

Validation completed: 348 Python tests passed together, followed by one additional passing regression for a quote that expires between account capture and submission, for 349 distinct tests. Forty-nine synthetic PowerShell launcher checks passed. These checks used fake broker responses, temporary ledgers and pure launcher function extraction. The whitespace check passed. No real broker orders, production inventory assignments or activation changes were made during this implementation, and the manual wrapper was not launched.

Read-only operating inspection found the Windows task Ready with LastTaskResult 0 and its next trigger at September 9, 03:55 Pacific. Existing activation controls were already enabled; no trader worker was running at inspection. The user remains responsible for the initial manual launch requested in this conversation.

The bounded supervision renewal helper stopped with no further calls pending. Root lease `ef16a296-35ee-4569-90f6-ac7cb3cdc0d4` was released through the native runtime at `2026-09-09T08:01:26.517052+00:00`, with matching expiry and update timestamps. No detached helper remains.
